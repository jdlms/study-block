services:
  study-blocks:
    build:
      context: .
      dockerfile: Dockerfile
    image: study-blocks:latest
    expose:
      - "8080"
    env_file:
      - .env
    volumes:
      - /opt/data/study-blocks:/app/data
    restart: unless-stopped
